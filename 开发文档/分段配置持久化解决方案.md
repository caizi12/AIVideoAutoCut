# 分段配置持久化问题解决方案

**问题**: 重启后分段配置丢失，每次都需要重新设置

**根本原因**: 配置参数没有保存到session中，也没有从session中恢复

---

## ✅ 已实施的解决方案

### 1. 后端保存配置到session

**修改文件**: `backend/services/broll_service.py`

**修改位置**: `_run_plan_task` 函数

**修改内容**:
```python
session.update({
    'aspect_ratio': data.get('aspect_ratio') or 'original',
    'subtitle_mode': data.get('subtitle_mode') or 'burned',
    'providers': data.get('providers') or list(self.DEFAULT_CONFIG['providers']),
    # 保存分段配置 ✅
    'min_shot_duration': float(data.get('min_shot_duration', 3.0)),
    'max_shot_duration': float(data.get('max_shot_duration', 8.0)),
    'prefer_sentence_boundary': data.get('prefer_sentence_boundary', False),
    'shots': self._build_shots(subtitles, data)
})
```

**效果**: 生成方案时，配置参数会保存到数据库

---

### 2. 前端恢复配置

**修改文件**: `frontend/templates/subtitle_tool.html`

**修改位置**: `applyBrollControls` 函数

**修改内容**:
```javascript
function applyBrollControls() {
    const session = state.brollSession || defaultBrollSession();
    els.brollAspectRatio.value = session.aspect_ratio || 'original';
    els.brollSubtitleMode.value = session.subtitle_mode || 'burned';

    // 恢复分段配置 ✅
    if (session.min_shot_duration !== undefined) {
        els.brollMinDuration.value = session.min_shot_duration;
    }
    if (session.max_shot_duration !== undefined) {
        els.brollMaxDuration.value = session.max_shot_duration;
    }
    if (session.prefer_sentence_boundary !== undefined) {
        els.brollPreferSentence.checked = session.prefer_sentence_boundary;
    }

    // 更新模板按钮状态 ✅
    const detected = detectCustomConfig();
    document.querySelectorAll('.broll-template-btn').forEach(btn => {
        btn.classList.toggle('active', btn.dataset.template === detected);
    });
}
```

**效果**: 加载项目时，配置参数会从数据库恢复到UI

---

### 3. 前端自动保存配置

**修改文件**: `frontend/templates/subtitle_tool.html`

**修改位置**: `saveBrollSessionSnapshot` 函数

**修改内容**:
```javascript
async function saveBrollSessionSnapshot(immediate = false) {
    if (!state.projectId || !state.brollSession) return;
    const run = async () => {
        try {
            state.brollSession.aspect_ratio = els.brollAspectRatio.value;
            state.brollSession.subtitle_mode = els.brollSubtitleMode.value;

            // 保存分段配置 ✅
            const config = getBrollConfig();
            state.brollSession.min_shot_duration = config.min_shot_duration;
            state.brollSession.max_shot_duration = config.max_shot_duration;
            state.brollSession.prefer_sentence_boundary = config.prefer_sentence_boundary;

            await apiJson(`/api/broll/session/${encodeURIComponent(state.projectId)}`, {
                method: 'POST',
                headers: {'Content-Type': 'application/json'},
                body: JSON.stringify(state.brollSession)
            });
        } catch (error) {
            console.warn('保存补画面工作区失败', error);
        }
    };
    // ...
}
```

**效果**: 修改配置时，自动保存到数据库

---

### 4. 添加自动保存触发器

**修改文件**: `frontend/templates/subtitle_tool.html`

**添加事件监听器**:

```javascript
// 1. 点击模板按钮时自动保存
document.querySelectorAll('.broll-template-btn').forEach(btn => {
    btn.addEventListener('click', () => {
        applyBrollTemplate(btn.dataset.template);
        saveBrollSessionSnapshot(false);  // ✅ 自动保存
    });
});

// 2. 配置输入变化时自动保存
[els.brollMinDuration, els.brollMaxDuration, els.brollPreferSentence].forEach(el => {
    el.addEventListener('change', () => {
        const detected = detectCustomConfig();
        document.querySelectorAll('.broll-template-btn').forEach(btn => {
            btn.classList.toggle('active', btn.dataset.template === detected);
        });
        saveBrollSessionSnapshot(false);  // ✅ 自动保存
    });
});

// 3. 重置配置时自动保存
els.brollResetConfig.addEventListener('click', () => {
    applyBrollTemplate('default');
    saveBrollSessionSnapshot(false);  // ✅ 自动保存
    setStatus('已重置为默认配置', 'success');
});

// 4. 应用配置时立即保存
els.brollApplyConfig.addEventListener('click', async () => {
    // ...
    await saveBrollSessionSnapshot(true);  // ✅ 立即保存
    // ...
});
```

**效果**: 任何配置修改都会自动保存

---

## 🔄 完整的配置生命周期

### 1. 首次生成方案

```
用户操作: 点击"生成补画面方案"
    ↓
前端: 读取配置 (min=3.0, max=8.0, prefer=false)
    ↓
前端: 发送到后端 (createBrollTask)
    ↓
后端: 使用配置生成镜头
    ↓
后端: 保存配置到session
    ↓
数据库: 存储配置
```

---

### 2. 修改配置

```
用户操作: 点击"快节奏"模板
    ↓
前端: 更新UI (min=2.0, max=5.0, prefer=false)
    ↓
前端: 自动保存到session (saveBrollSessionSnapshot)
    ↓
数据库: 更新配置
```

---

### 3. 应用新配置

```
用户操作: 点击"应用配置并重新分段"
    ↓
前端: 立即保存配置 (saveBrollSessionSnapshot(true))
    ↓
前端: 发送到后端 (runBrollTask)
    ↓
后端: 使用新配置重新生成镜头
    ↓
后端: 保存新配置到session
    ↓
数据库: 更新配置和镜头
```

---

### 4. 重启/刷新页面

```
用户操作: 刷新页面或重启应用
    ↓
前端: 加载项目 (restoreSession)
    ↓
前端: 加载broll session (loadBrollSession)
    ↓
前端: 恢复配置到UI (applyBrollControls)
    ↓
UI: 显示之前的配置 ✅
```

---

## 📊 数据库存储结构

**broll_sessions 表**:
```json
{
  "project_id": "xxx",
  "version": 1,
  "source_video_path": "/path/to/video.mp4",
  "aspect_ratio": "original",
  "subtitle_mode": "burned",
  "providers": ["wikimedia", "pexels", ...],

  // 分段配置 ✅
  "min_shot_duration": 2.0,
  "max_shot_duration": 5.0,
  "prefer_sentence_boundary": false,

  "shots": [...],
  "rendered_video_url": "",
  "license_manifest_url": "",
  "updated_at": "2026-05-28T12:00:00"
}
```

---

## 🎯 测试验证

### 测试步骤

1. **设置配置**:
   - 点击"快节奏"模板
   - 确认配置: min=2.0, max=5.0, prefer=false

2. **生成方案**:
   - 点击"应用配置并重新分段"
   - 等待完成

3. **刷新页面**:
   - 按 F5 或 Cmd+R
   - 等待页面加载

4. **验证配置**:
   - 展开配置面板
   - 检查配置值是否保持
   - 检查"快节奏"按钮是否高亮

5. **预期结果**:
   - ✅ 配置值保持不变
   - ✅ 模板按钮正确高亮
   - ✅ 镜头数量保持不变

---

### 测试场景

#### 场景1: 修改配置后刷新

```
1. 设置快节奏 (min=2.0, max=5.0)
2. 应用配置
3. 刷新页面
4. 验证: 配置应该是 min=2.0, max=5.0 ✅
```

#### 场景2: 切换项目

```
1. 项目A: 设置快节奏
2. 项目B: 设置慢节奏
3. 切换回项目A
4. 验证: 配置应该是快节奏 ✅
```

#### 场景3: 重启应用

```
1. 设置自定义配置 (min=3.5, max=7.0)
2. 应用配置
3. 关闭应用
4. 重新启动应用
5. 打开项目
6. 验证: 配置应该是 min=3.5, max=7.0 ✅
```

---

## 🎉 总结

### ✅ 已解决的问题

1. **配置持久化** ✅
   - 配置保存到数据库
   - 每个项目独立配置

2. **自动保存** ✅
   - 点击模板自动保存
   - 修改配置自动保存
   - 应用配置立即保存

3. **自动恢复** ✅
   - 刷新页面自动恢复
   - 切换项目自动恢复
   - 重启应用自动恢复

4. **UI同步** ✅
   - 配置值正确显示
   - 模板按钮正确高亮
   - 自定义检测正确

---

### 📊 用户体验提升

| 功能 | 修改前 | 修改后 |
|-----|--------|--------|
| 配置持久化 | ❌ 丢失 | ✅ 保存 |
| 刷新页面 | ❌ 重置 | ✅ 保持 |
| 切换项目 | ❌ 混乱 | ✅ 独立 |
| 自动保存 | ❌ 手动 | ✅ 自动 |

---

### 🔧 技术亮点

1. **项目级配置** - 每个项目独立配置
2. **自动保存** - 无需手动保存
3. **智能恢复** - 自动检测配置类型
4. **UI同步** - 配置和UI实时同步

---

## 📁 修改的文件

1. **后端**:
   - `backend/services/broll_service.py`
     - `_run_plan_task`: 保存配置到session

2. **前端**:
   - `frontend/templates/subtitle_tool.html`
     - `applyBrollControls`: 恢复配置
     - `saveBrollSessionSnapshot`: 保存配置
     - 事件监听器: 自动保存触发器

---

## 🚀 立即测试

1. **清除浏览器缓存** (Cmd+Shift+R)
2. **上传视频并生成字幕**
3. **设置快节奏配置**
4. **应用配置并生成方案**
5. **刷新页面**
6. **验证配置是否保持**

---

**状态**: ✅ 已完成
**测试**: 待验证
